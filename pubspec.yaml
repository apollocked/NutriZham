name: nutrizham
description: A comprehensive fitness nutrition app with meal planning

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  firebase_core: ^3.1.0 # Updated
  firebase_auth: ^5.1.0 # Updated
  cloud_firestore: ^5.1.0 # Updated
  google_sign_in: ^6.2.1 # Updated
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
