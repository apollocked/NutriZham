name: nutrizham
description: A comprehensive fitness nutrition app with meal planning

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  firebase_core: any # Let Flutter choose compatible version
  firebase_auth: any # Let Flutter choose compatible version
  cloud_firestore: any
  google_sign_in: ^6.1.5
  crypto: ^3.0.3
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
